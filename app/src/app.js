import content from './content-utils.js';

function getContent(blockName) {
  /*
  switch (blockName) {
    case 'greeting':
      return content.getGreeting('anonymous');
    case 'time':
      return `it's ${content.getTime()}`;
    default:
      return ''
  }
  */
}

function show(blockName) {
  /*
  const domElement = document.getElementById(blockName);
  domElement.innerHTML = getContent(blockName);
  */
}

show('greeting');
show('time');